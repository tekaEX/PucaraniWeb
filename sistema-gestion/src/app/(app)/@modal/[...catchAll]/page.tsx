// Cierra el modal al navegar (soft-nav) a cualquier ruta que no sea una
// creación interceptada — p. ej. el botón "Cancelar" de los formularios.
export default function CatchAll() {
  return null;
}
